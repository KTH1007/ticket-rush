# Gradle 데몬(빌드 오케스트레이션)과 Kotlin 컴파일 데몬(kapt 포함) 둘 다
# 기본 Metaspace 한도(384m)가 작아서, kapt + detekt + ktlint를 같이 쓰는
# 빌드를 반복하면 OutOfMemoryError: Metaspace로 데몬이 죽는다.
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=1g
kotlin.daemon.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=1g
